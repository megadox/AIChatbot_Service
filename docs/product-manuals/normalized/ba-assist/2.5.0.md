# BATEM Online Manual

- Product: BA-Assist
- Version: 2.5.0
- Source URL: https://docs.batem.com/b_manual/b_assist_2.5.0.html

## 사용자 매뉴얼 (BA Assist 2.5.X)

- 01. BA Assist란 1-1. BA Assist와 인터페이스
- 1-2. BA Assist의 파일형식
- 1-3. BA Assist의 기능 1-3-1. 스케줄러 기능
- 1-3-2. 패키지파일 등록/생성 기능
- 1-3-3. 상태 보기
- 1-3-4. 기타 부가 기능
- 1-3-5. 대쉬보드
- 1-3-6. Calendar
- 1-4. BATEM V2.0 시스템 권장사항
- 02. 환경 설정 2-1. BA Assist 설치방법
- 03. 스케줄러 3-1. 태스크(=fpk 또는 fpx파일) 로딩하기 3-1-1. fpx 파일 변환 후, 로딩하기
- 3-1-2. fpk 파일 변환 후, 로딩하기
- 3-1-3. 태스크 업데이트하기
- 3-2. 스케줄 설정하기 3-2-1. Schedule 창 열기
- 3-2-2. Trigger 시간 설정하기
- 3-2-3. 스케줄 옵션 설정하기
- 3-2-4. PC 셧다운 여부 설정하기
- 3-2-5. 스케줄 활성화하기
- 04. 메인 메뉴 4-1. Schedule List
- 4-2. Log
- 4-3. Stop
- 4-4. Setting 4-4-1. BA Assist 옵션 설정 기능
- 4-4-2. BA Assist 큐 설정 기능
- 4-5. Packing
- 4-6. Sequential
- 4-7. Refresh
- 4-8. Close
- 4-9. Exit
- 4-10. Help
- 05. 시스템 트레이 메뉴 5-1. Task List
- 5-2. Logs
- 5-3. Task Schedule
- 5-4. Setting
- 5-5. Exit
- 06. 기타 편의기능 6-1. 스킨 설정 기능
- 6-2. View Panel 기능
- 6-3. 리본메뉴 최소화(Ctrl+F1)
- 6-4. Help 메뉴
screen_search_desktop Version: 2.5.X | history Date: 2024.03.29

### BA Assist

비에이템

## 사용자 매뉴얼

### 목차

- 01. BA Assist란

1-1. BA Assist와 인터페이스

1-2. BA Assist의 파일 형식

1-3. BA Assist의 기능 1-3-1. 스케줄러 기능 1-3-2. 패키지파일 등록/생성 기능 1-3-3. 상태 보기 1-3-4. 기타 부가 기능 1-3-5. 대쉬보드 1-3-6. Calendar

1-4. BATEM V2.0 시스템 권장사항

- 02. 환경 설정

2-1. BA Assist 설치방법

- 03. 스케줄러

3-1. 태스크(=fpk 또는 fpx파일) 로딩하기 3-1-1. fpx 파일 변환 후 로딩하기 3-1-2. fpk 파일 변환 후 로딩하기 3-1-3. 태스크 업데이트하기

3-2. 스케줄 설정하기 3-2-1. Schedule 창 열기 3-2-2. Trigger 시간 설정하기 3-2-3. 스케줄 옵션 설정하기 3-2-4. PC 셧다운 여부 설정하기 3-2-5. 스케줄 활성화하기

- 04. 메인 메뉴

4-1. Schedule List

4-2. Log

4-3. Stop

4-4. Setting 4-4-1. BA Assist 옵션 설정 기능 4-4-2. BA Assist 큐 설정 기능

4-5. Packing

4-6. Sequential

4-7. Refresh

4-8. Close

4-9. Exit

4-10. Help

- 05. 시스템 트레이 메뉴

5-1. Task List

5-2. Logs

5-3. Task Schedule

5-4. Setting

5-5. Exit

- 06. 기타 편의기능

6-1. 스킨 설정 기능

6-2. View Panel 기능

6-3. 리본메뉴 최소화(Ctrl+F1)

6-4. Help 메뉴

01. BA Assist란

BA Assist는 BATEM의 RDA(Robotic Desktop Automation) 제품을 지칭한다. BA Studio(개발 도구)에서 작성한 업무 자동화 태스크를 스케줄러를 통하여 사용자가 정의한 조건에 따라 실행하는 서비스를 수행한다. BA Assist는 지정된 시간에 자동화 태스크들을 실행하고 다양한 간격으로 실행을 반복 할 수 있다. BA Assist는 여러 사용자, 여러 부서의 응용 프로그램에 해당하는 넓은 범위에 걸쳐서 작동하는 BA Server 시스템과 달리 오직 하나의 PC(Personal Computer)에서만 작동하며 해당 사용자가 작업하는 응용 프로그램들과의 상호 작용만 담당한다.

#### 1-1. BA Assist와 인터페이스

BA Assist는 단일 사용자를 위한 업무 자동화를 제공하여 반복적인 수동 작업의 일들을 대신할 수 있다.

![BA Assist와 인터페이스](../assets/ba-assist/2.5.0/assist_img00-84b1987194cadace.png)

BA Studio에 의해서 작성된 후 BA Assist의 스케줄링에 포함되는 자동화 태스크의 수행 목록은 작업자의 할 일 목록 또는 체크 리스트의 항목에 해당된다고 할 수 있으며, BA Assist에 의해 실행되는 순차적인 일의 처리과정은 자동으로 항목을 수행하고 체크리스트 위에서 완료되었음을 체크해 나가는 것으로 비유 할 수 있다.

#### 1-2. BA Assist 파일 형식

BA Studio(개발 도구)에서 만들어진 업무 자동화 태스크는 태스크 파일(fp)과 프로젝트 파일(fpp) 두 가지 파일 형식으로 저장된다.

| 파일형식 | 설명 |
| --- | --- |
| ① fp파일(*.fp) | BA Studio에서 업무 자동화를 위한 태스크를 생성하여 구현한 실행파일로서, 파일 확장자는 fp이다. |
| ② fpp파일(*.fpp) | BA Studio에서 업무 자동화를 위한 프로젝트를 생성하여 구현한 실행파일로서, 파일 확장자는 fpp이다. |

위 업무 자동화 파일들을 BA Assist(RDA 제품)에서 구동하게 하려면 fpk(fp파일+리소스) 또는 fpx(fpp파일+리소스)의 확장자를 가진 패키지 파일로 변환되어야 한다.

| 파일형식 | 설명 |
| --- | --- |
| ① fpk 파일(*.fpk) | BA Studio에서 만들어진 업무자동화 태스크인 fp파일과 리소스를 묶어 패키지로 만든 파일이다. **BA Assist를 통해 fpk파일을 생성한다. |
| ② fpx 파일(*.fpx) | BA Studio의 Project 만들기로 생성된 Project 폴더를 패키지로 변환한 파일이다. Project 폴더는 Project실행 파일인 fpp파일과 리소스 폴더들을 포함하고 있다. **BA Studio를 통해 fpx파일을 생성한다. |

실행파일과 리소스를 함께 관리하기 위해 패키지 파일을 생성하는 두 가지 경로 를 아래의 그림을 통해 나타내었다.

![패키지 파일 생성 과정](../assets/ba-assist/2.5.0/assist_img00-2-3894bfb83867f89b.png)

#### 1-3. BA Assist의 기능

BA Assist는 한 개 이상의 자동화 태스크 (fpk 또는 fpx파일)들을 정해진 시간에 수행되도록 하는 스케줄러의 일을 하는 것이 주된 임무이다. 또한, 태스크 로딩기능으로 서버로부터 자동화 태스크를 다운 받아 로딩하는 기능, fp파일들을 패키지 파일인 fpk로 만드는 기능, BA Studio의 Export 기능에 의해서 하나의 project가 변환된 fpx파일을 읽어 들여 로딩하는 기능 등을 지원하고 있고, 상태메뉴 보기 기능으로 스케줄에 등록된 태스크들만 추출하여 만든 스케줄 리스트를 검색하는 기능과 로그창을 통해 수행이력 및 로그메시지를 검색하는 기능 등을 제공하고 있다. 그 밖에 Short Cut기능을 통해 단축키를 이용하여 특정 태스크를 즉시 수행할 수 있는 기능과 편의기능으로 스킨 설정 기능 등을 제공하고 있다.

##### 1-3-1. 스케줄러 기능

- 1) 스케줄러는 스케줄에 등록한 시간까지 대기 한 다음 이벤트를 발생시켜 저장된 태스크를 수행하게 한다.
- 2) 지정된 시간에 자동화된 일의 프로세스를 실행하고 다양한 간격으로 실행을 반복 할 수 있다. 일을 한 번 실행하거나 시간 간격을 두고 매분, 매시간, 매일, 매주, 매월 또는 매년 반복하게 하도록 구성할 수 있다.
##### 1-3-2. 패키지파일 등록/생성 가능

- 1) BA Studio의 export기능을 통해 Project폴더를 패키지 파일인 fpx로 변환한 후 BA Assist에 등록하여 실행할 수 있는 기능
- 2) BA Studio에서 만들어진 자동화 태스크인 fp파일과 리소스를 묶어 BA Assist를 통해 패키지 파일인 fpk파일을 생성하고 수행하는 기능
- 3) BA Server에 등록되어 있는 패키지 파일인 fpk 또는 fpx파일을 다운받아 BA Assist에서 수행시키는 기능 등을 제공하고 있다.
##### 1-3-3. 상태 보기

- 메인 메뉴 아래 「Schedule」과 「Log」 의 서브 메뉴를 통해 스케줄러에 등록된 일들과 실행의 로그 상태 등을 볼 수 있는 기능을 제공하고 있다. 또한 「Setting」메뉴의 「Dashboard Set」 기능을 통하여 특정 서버와의 DB 동기화를 수행할 수 있으며 특정 서버에 접속하여 Dashboard 보기를 통하여 태스크의 스케줄링과 수행 상태를 모니터링 할 수 있다.
##### 1-3-4. 기타 부가 기능

- 1) 세팅 메뉴를 통해 멀티프로세싱 및 Queue 사용 여부를 설정할 수 있는 기능
- 2) 시스템 트레이 메뉴를 통해 BA Assist의 메인 메뉴로 이동하거나 특정 부가 메뉴로 이동하는 기능
- 3) 사용자 인터페이스인 테마를 여러 가지 형태로 변경할 수 있는 기능 등을 제공하고 있다.
- 4) Sequential 태스크 수행을 위한 Sequential 태스크 설정 기능
##### 1-3-5. 대쉬보드

- 1) 선택한 작업의 실행 통계를 확인할 수 있는 기능
- 2) 성공률 및 최근 작업 수행 시간을 확인할 수 있다.
##### 1-3-6. Calendar

- 예정 스케줄과 이미 수행된 스케줄을 달력화면에서 확인할 수 있는 기능
#### 1-4. BATEM V2.0 시스템 권장사항

| 소프트웨어 |
| --- |
| 운영체제 | BA Assist용 PC | Microsoft Windows 10 Pro – 64bit |
| BA Studio용 PC |
| BA-Worker용 PC |
| BA Server접속 PC |
| BA Server | Ubuntu 18.04 – 64bit |
| 특이사항 (제품구동요구사항) | BA Studio용 PC | - 프레임워크: .Net Framework 4.8 이상 - 오피스 프로그램: MS Excel 2007 이상, MS Word 2007 이상, MS Outlook 2007 이상 - 웹 브라우저: Microsoft Internet Explorer 최신 버전, Google Chrome 최신버전 (Driver2.3이상) |
| BA Assists용 PC |
| BA-Worker용 PC |
| BA Server 접속 PC | - 프레임워크: .Net Framework 4.5 이상 - 웹 브라우저: Google Chrome 최신 버전 |
| BA Server | - DBMS : MySQL 5.7 - JAVA: Oracle JDK 1.8 이상 - Web Server : Apache 2.4 - WAS : Tomcat 8.5 |
| 하드웨어 |
| 하드웨어 사양 | BA Studio용 PC | CPU: Intel i5 Dual Core 2.0GHz, MEM: 8GB, HDD: 256GB |
| BA Assist용 PC |
| BA-Worker용 PC |
| BA Server접속 PC |
| BA Server접속 PC | CPU: 2 CPUs, MEM: 4GB, SSD: 80GB |
| 네트워크 환경 | TCP/IP 환경 |
| 기타 환경 | N/A |
| 기타사항 |
| 데이터 암복호화: One-way Bcrypt |

02. 환경 설정

#### 2-1. BA Assist 설치방법

BATEM에서 제공하는 Web Site에서 BA Assist 설치파일을 다운로드 받아서 설치할 수 있다. 아래의 Step별 설치과정을 따라서 BA Assist를 설치하도록 한다.

- Step1.

https://download.batem.com 에 접속하여 나타난 아래의 BA Assist 다운로드 창에서 왼쪽 DOWNLOAD 버튼을 눌러 설치 파일인 BAssistSetup.exe을 다운 받는다.

- Step2.

아래의 과정에 따라 설치한다. ① 추가 정보 클릭 ② 실행 클릭 ③ 설치 시작 추가 정보를 클릭한다. 실행 버튼을 클릭한다. 다음 버튼을 클릭한다. ④ 설치 폴더 선택 ⑤ 설치 진행 ⑥ 설치 완료 설치 폴더를 선택한다. 설치를 진행한다. 마침을 클릭하여 Assist 설치를 마무리한다.

- Step3.

아래의 과정에 따라 라이선스 절차를 확인 후 등록한다. *정책에 따라 변경 가능하다. ① Register License 클릭 ② License Information 복사 ③ License 담당자에게 메일발송 설치가 끝난 후 라이선스 등록화면이 나오면 Register License 버튼을 누른다. License > Create License Info. 탭으로 들어간 후 License Information 아래에 있는 내용을 복사한다. 라이선스 담당자(license@batem.com)에게 복사한 내용과 직급을 입력 후메일을 발송하여 import할 라이선스 파일을 요청한다. ④ Import License File 클릭 ⑤ License File 열기 ⑥ 라이선스 등록 완료 담당자에게 요청한 파일이 오면 파일을 다운로드 후 Import License File 버튼을 클릭한다. 라이선스 담당자에게 받은 License File의 경로를 확인 후 열기를 누른다. Register License 버튼을 클릭하여 라이선스 등록을 마무리한다.

03. 스케줄러

BA Assist의 주요 기능은 스케줄러이다. 스케줄러는 사용자가 정의한 조건에 따라 BA Studio에서 작성한 자동화 태스크를 실행하는 서비스로, BA Assist는 지정된 시간에 자동화 태스크를 실행하고 다양한 간격으로 실행을 반복 할 수 있다.

#### 3-1. 태스크(=fpk 또는 fpx 파일) 로딩하기

아래의 세가지 형태의 자동화 태스크들을 BA Assist에 로딩하여 수행할 수 있다.

- 1) BA Studio의 export기능을 통해 Project폴더를 패키지 파일인 fpx로 변환한 후 BA Assist에 로딩하기
- 2) BA Studio에서 만들어진 태스크인 fp파일과 태스크와 관련된 리소스파일을 소스로 하여 패키지 파일인 fpk파일을 생성한 후 BA Assist에 로딩하기
- 3) BA Server에 등록되어 있는 패키지 파일인 fpk 또는 fpx파일을 다운받아 BA Assist에 로딩하기
☞ fpx와 fpk파일 형식에 관해서는 BA Assist 실행 태스크 파일의 형식 을 참조한다.

##### 3-1-1. fpx 파일 변환 후, 로딩하기

BA Assist 파일 형식 에서 다루었던 것처럼, BA Studio(개발 도구)에서 작성된 fpp파일(프로젝트 파일)을 BA Assist에서 구동하려면 fpp파일과 리소스를 패키징하여, fpx파일(패키지 파일)로 변환하는 과정이 필요하다. 본 작업을 수행해보자.

Step1 을 통해 BA Studio에서 fpp파일을 리소스와 패키징하여 fpx파일로 변환하는 과정을 다룬다.

Step2 을 통해 fpx파일을 BA Assist에 로딩하는 과정을 나타내기로 한다.

Step1. fpx 파일로의 변환

- Step1-1.

Sample Tasks 폴더> 현금예산작성하기 폴더> 『현금예산작성하기.fpp』 파일을 열고 상단의 Project 탭에서 Export Project를 선택한다.

- Step1-2.

Sample Tasks 폴더로 저장 경로를 설정하고, 파일 형식(.fpx)을 확인 한 후, 파일 이름에 『현금예산작성하기_패키지』를 입력한다. ☞ 주의: 이 때, fpx파일의 저장 위치를 지정할 때 fpp 파일이 위치한 폴더는 허용되지 않는다. 따라서 프로젝트 폴더(예, 현금예산작성하기) 외부에 있는 저장 폴더(예, Sample Tasks)를 지정하여 변환된 fpx파일이 저장되도록 해야 한다.

- Step1-3.

버튼을 누른다.

- Step1-4.

해당 폴더에 패키지 파일인 fpx 파일이 생성되었음을 확인할 수 있다.

Step2. 변환된 fpx 파일 로딩

- Step2-1.

BA Assist의 스케줄 에디터 화면에서 왼쪽 상단의 Add Task를 선택한다.

- Step2-2.

Step1에서 만들어 놓았던 『현금예산작성하기_패키지.fpx』파일을 선택 후, 버튼을 누른다.

- Step2-3.

아래의 화면처럼 선택된 fpx파일이 BA Assist에 로딩되었음을 확인할 수 있다.

##### 3-1-2. fpx 파일 변환 후, 로딩하기

앞서 다루었던 것처럼, BA Studio(개발 도구)에서 작성된 fp파일(태스크 파일)을 BA Assist에서 구동하려면 fp파일과 리소스를 패키징하여, fpk파일(패키지 파일)로 변환하는 과정이 필요하다. 본 작업을 수행해보자.

Step1 을 통해 BA Assist에서 fp파일과 리소스를 패키징하여, fpk파일로 변환하는 과정을 다룬다.

Step2 을 통해 fpk파일을 BA Assist에 로딩하는 과정을 나타내기로 한다.

Step1. fpk 파일로의 변환

- Step1-1.

Option 탭의 누른 후 나타난 아래의 화면에서 〓를 선택한다.

- Step1-2.

fpk로 변환할 대상 폴더를 선택한다. Sample Tasks 폴더의 『상품몰검색』 폴더를 가져온다.

- Step1-3.

아래의 그림을 참고하여, 선택한 폴더에서 Main Task에 해당하는 fp파일에 체크 박스를 클릭한 후, Package Name과 Description 을 입력한다. - Main Task : 상품몰검색\쇼핑몰상품정보가져오기.fp - Package Name: 『상품정보크롤링 - Description: 『상품정보를 인기순 검색 후 특정 상품정보필드만 선택하여 엑셀에 저장한다.』

- Step1-4.

입력 과정이 완료되면 버튼을 누른다.

- Step1-5.

Sample Tasks 폴더로 저장 경로를 선택한 후, 파일 이름(N)에 『상품정보크롤링』을 입력하고 버튼을 누른다.

- Step1-6.

해당 폴더에서 패키지 파일인 fpk 파일이 생성되었음을 확인할 수 있다. ☞ 참고로, 아래의 그림처럼 〓버튼을 눌러 이미 만들어진 fpk 파일을 불러와서 패키지 정보를 수정할 수도 있다. 즉, 〓버튼 동작 이후의 시퀀스는 패키지를 업데이트하는 기능으로 작동하는 것으로 간주할 수 있다.

Step2. fpk파일을 로딩

- Step2-1.

BA Assist의 스케줄 에디터 화면에서 왼쪽 상단에 Add Task를 클릭한다.

- Step2-2.

파일 검색창에서 Step 1에서 만들어 놓은 fpk파일을 선택한 후 버튼을 누른다.

- Step2-3.

아래의 그림처럼 fpk 파일이 BA Assist에 로딩된 화면이 나타난다.

##### 3-1-3. 태스크 업데이트하기

BA Assist에 이미 로딩된 태스크를 업데이트하는 과정은 다음과 같다.

- Step1.

BA Assist의 스케줄 에디터 화면에서 왼쪽 상단에 Add Task를 클릭한다.

- Step2.

3-1-1. fpx 파일 변환 후, 로딩하기의 Step2에서와 마찬가지로 파일 검색창에서 업데이트하고자 하는 fpk파일을 선택한 후 버튼을 누른다.

- Step3.

팝업 창에 YES 버튼을 클릭한다.

#### 3-2. 스케줄 설정하기

봇이 수행할 태스크를 로딩하였으니, 스케줄을 설정해보자. 이 때, 3-1의 ①번과 ②번에서 만들어 놓은 예시를 활용할 것이다.

##### 3-2-1. Schedule 창 열기

- Step1.

스케줄을 설정하고자 하는 태스크를 선택 > Schedule 버튼 (①번 버튼) 또는 + 버튼 (②번 버튼)을 클릭한다.

- Step2.

Step1의 과정을 수행하면, 스케줄 설정 화면(Schedule 창)이 나타난다.

※ 다음에서는 본 Schedule 창에서 Trigger 시간(시작 시간), 스케줄 옵션, PC 셧다운 여부를 설정하고, 스케줄을 활성화시키는 작업을 수행할 것이다.

##### 3-2-2. Trigger 시간 설정하기

Trigger 기능은 실행하고자 하는 태스크가 시작될 시간과 날짜를 의미한다.

- → 날짜의 경우, 직접 키보드의 숫자키를 입력하거나 이동키 위, 아래 버튼을 이용하여 시간을 조정할 수 있다. 또한, 달력 아이콘을 클릭하여 표시된 선택 메뉴에서 날짜를 고를 수도 있다.
- → 시간의 경우, 직접 키보드의 숫자키를 입력하거나 이동키 위, 아래 버튼을 이용하여 시간을 조정할 수 있다.
![250_assist_img7](../assets/ba-assist/2.5.0/250_assist_img7-3a37811e3c2fbe7a.png)

##### 3-2-3. 스케줄 옵션 설정하기

스케줄 옵션 기능은 다양한 시간 간격으로 자동화 태스크의 실행을 반복 할 수 있게 하는 기능이다. Times창, Option창에 해당한다.

![250_assist_img8](../assets/ba-assist/2.5.0/250_assist_img8-e0d54a40c1dc3ff4.png)

반복 주기 설정

위 화면 Times 창을 통해 One Time(한 번만), Daily(매일마다), Weekly(매주마다), Monthly(매월마다) 중에서 하나를 택하여 반복주기를 설정할 수 있다. 각각의 메뉴는 아래의 하위메뉴를 통하여 세부 설정기능을 제공하고 있고, 특히, Mothly 설정에는 day(일) 설정과 day of the week(주단위) 설정 기능이 더 들어 있다.

| One Time | Daily |
| --- | --- |
| [동작설명] ● Trigger에 설정된 날짜와 시간부터 ● Period에 설정된 시간 동안 ● Job Repeat Interval에 설정된 시간마다 태스크를 수행한다. 따라서, Trigger에 설정된 시간부터 Period에 설정된 시간 경과 후에는 더 이상 태스크를 반복 수행하지 않는다. | [동작설명] ● Trigger에 설정된 날짜와 시간부터 ● Recur every에 설정된 일 간격으로 ● Period에 설정된 시간 동안 ● Job Repeat Interval에 설정된 시간마다 태스크를 수행한다. 따라서, Trigger에 설정된 시간부터 Period에 설정된 시간 동안 수행한 횟수만큼 매일 태스크를 반복 수행한다. |
| Weekly | Monthly(I) |
| [동작설명] ● Trigger에 설정된 날짜와 시간부터 ● Recur every에 설정된 주 간격으로 ● every day of the week에 설정된 요일마다 (복수선택 가능) ● Period에 설정된 시간 동안 ● Job Repeat Interval에 설정된 시간마다 태스크를 수행한다. 따라서, Trigger에 설정된 시간에 처음 수행한 태스크는 설정된 주 단위로 반복 수행을 한다. 이 때 Recur every에 설정할 수 있는 최대값은 52 (단위: 주)이다. | [동작설명 - day 모드가 선택된 경우] ● Trigger에 설정된 날짜와 시간부터 ● Month에 설정된 월마다 (복수선택 가능) ● day에 설정된 날짜마다 (복수선택 가능) ● Period에 설정된 시간 동안 ● Job Repeat Interval에 설정된 시간마다 태스크를 수행한다. 따라서, Trigger에 설정된 시간에 처음 수행한 태스크는 설정된 월 마다 반복 수행을 한다. |
| Monthly(II) | 스케줄 겹침 처리 방법 |
| [동작설명 – day of the week 모드가 선택된 경우] ● Trigger에 설정된 날짜와 시간부터 ● Month에 설정된 월마다 (복수선택 가능) ● day of the week에 설정된 주 M (복수선택 가능) 요일 N (복수선택 가능) 에 대하여 M번째 주 N요일에 ● Period에 설정된 시간 동안 ● Job Repeat Interval에 설정된 시간마다 태스크를 수행한다. 따라서, Trigger에 설정된 시간에 처음 수행한 태스크는 설정된 월 마다 반복 수행을 한다. | 이미 동작 중인 작업이 있을 경우 처리 방법을 설정한다. [동작설명 - day 모드가 선택된 경우] ● IgnoreNew: 새로 진행되는 작업을 무시한다. ● Parallel: 병행(병렬)으로 작업을 진행한다. ● Queue: 큐에 쌓는다. ● StopExisting: 기존 작업을 정지한다. ※Paralled 은 동시에 작업이 진행됨으로 오류가 발생할 확률이 높다. 권장하지 않는다. ※Queue 사용 시 Setting에서 Queue사용을 체크해야한다. |

##### 3-2-4. PC 셧다운 여부 설정하기

해당 자동화 태스크가 종료된 후 PC를 셧다운 하고자 할 때, Shutdown PC after execution 체크 박스에 클릭한다.

![250_assist_img15](../assets/ba-assist/2.5.0/250_assist_img15-6fd702f80305e963.png)

☞ Option 아래 Job Repeat Interval 과 Period가 설정되어 태스크가 반복적으로 수행되는 경우의 PC 셧다운동작은 Trigger에 설정된 시간 또는 Job Repeat Interval 설정에 의한 반복 수행 중 첫 번째 스케줄 이벤트 수행 후 이루어진다.

##### 3-2-5. 스케줄 활성화하기

아래의 화면에서 Enabled 체크 박스를 클릭하면, 예약된 스케줄을 활성화할 수 있다. 체크 박스가 선택 해제되어 있다면, 예약된 스케줄은 작동되지 않는다.

![250_assist_img16](../assets/ba-assist/2.5.0/250_assist_img16-6c5e2a5f73fdd7b5.png)

04. 메인 메뉴

사용자의 사용 편의성을 증진시키기 위해, BA Assist 상단에는 주요 기능에 대한 상태 보기 메뉴가 위치하고 있다. 본 메뉴는 스케줄러의 등록 상태, 단축키의 등록 상태, 로그 요약 등의 기능을 제공하고 있다.

![250_assist_img17](../assets/ba-assist/2.5.0/250_assist_img17-3a311418f41f3b4c.png)

#### 4-1. Schedule List

Main 탭의 기능을 살펴보자. Schedule List 버튼을 누르면, Task Schedules 창이 나타난다. Task 창에서 스케줄이 설정된 리스트 만을 추출하여 보여주는 기능을 제공한다.

![assist_img27](../assets/ba-assist/2.5.0/assist_img27-02e1ef586ed1cb51.png)

| 메뉴 | 표시 화면 |
| --- | --- |

Export, Import 버튼을 사용하여 스케줄을 파일로 내보내기/불러오기가 가능하다.

※스케줄 설정 방법에 관해서는 위에서 설명된 스케줄 설정하기 를 참조한다.

#### 4-2. Log

Main 탭의 기능을 살펴보자. Log 아이콘을 누르면 아래와 같이 태스크의 수행 이력을 보여주는 화면이 나타난다. 수행 시간, 성공 여부를 확인 가능하며, 실패 시에는 로그 메시지가 함께 제공된다.

![assist_img46_3](../assets/ba-assist/2.5.0/assist_img46_3-1fb312e8d70b7b89.png)

| 메뉴 | 표시 화면 |
| --- | --- |

※이 때, Task 필드에 태스크 이름의 일부 또는 전체를 입력하고 검색 버튼을 누르면 검색어가 포함된 일부 태스크만의 이력을 추출하여 보여주는 기능도 포함되어 있다.

#### 4-3. Stop

Main 탭의 기능은 수동으로 동작한 Task의 작동을 정지시킨다.

![assist_img46_4](../assets/ba-assist/2.5.0/assist_img46_4-d0606eeb3c789456.png)

#### 4-4. Setting

Main 탭의 기능은 로그 옵션 설정 기능, 태스크 저장 폴더를 변경하는 기능, 키보드와 마우스 제어 기능, 대시보드 설정 기능, 큐 설정 기능 등을 제공한다.

![assist_img46_5](../assets/ba-assist/2.5.0/assist_img46_5-29d9625e158049e4.png)

##### 4-4-1. Assist 옵션 설정 기능

![250_assist_img18](../assets/ba-assist/2.5.0/250_assist_img18-44a768b25b8b9dc8.png)

[Setting Assist Configuration 탭 참조]

1) 화면 보호기 방지 기능

BA-Assist를 켜놓을 경우(숨김 포함) 화면 보호기로 진입을 방지할 수 있다. (체크 시: 화면 보호기 끄기)

2) 대쉬보드 사용 기능

「Setting」 메뉴에서 ① 「Use Dashboard」를 체크하고 ②「Dashboard Set」버튼을 클릭하여 서버 연결정보를 입력한 후 Dashboard DB 연결 확인과 DB 동기화를 수행할 수 있다.

- Step1.

「Use Dashboard」를 체크한다.

- Step2.

Dashboard Set 버튼을 클릭하여 Dashboard 접속을 위한 정보를 입력 후 「Dashboard Information」의 *BASE Url, *Sub Path를 입력하고 Remote Task 사용 여부를 체크한다.

- Step3.

Connection Test을 클릭하여 연결 시험을 진행한다. 연결이 실패하면 Step2의 정보를 정확히 입력하였는지 확인한 후 재실행한다.

- Step4.

「RDA Information」의 *Nick Name 필드에 닉네임을 입력한다.

- Step5.

「RDA Information」의 Create RDA ID 버튼을 클릭하면 RDA ID가 자동 생성되어 *RDA ID 필드 안에 입력된다.

- Step6.

설정을 완료하기 위하여 Save 버튼을 클릭한다. ☞ 설정이 완료되고 BA Assist에 추가된 스케줄 기반의 태스크를 Dashboard를 통하여 모니터링하기 위해서 DB Sync. 버튼을 클릭한다. DB Sync. 버튼의 클릭은 BA Assist와 Dashboard의 동기화를 수행하게 한다. ※대시보드의 화면은 서버 페이지 접속을 통해 이루어지며 접속 페이지 정보 업데이트는 고객사/운영사가 관리한다.

3) 키보드와 마우스 제어 기능

태스크를 수행하는 동안에 키보드와 마우스의 동작에 의한 오동작을 방지하는 기능이다.

![250_assist_img20](../assets/ba-assist/2.5.0/250_assist_img20-8b4bac0a41ae8d99.png)

체크 박스를 클릭할 경우, 태스크가 수행되는 동안 사용자의 키보드 입력과 마우스 입력을 방지할 수 있고 Esc 버튼을 두 번 눌러 방지 모드를 해제할 수 있다. 체크 박스가 Uncheck 상태이면 사용자의 키보드 또는 마우스 입력에 의해 태스크 실행 중 오동작이 발생할 가능성이 있다.

4) 스크린 녹화 기능

작업 수행시 주 스크린 화면이 동영상으로 녹화할 수 있다. (체크 시: 스크린 녹화 기능 끄기)

5) 스크린 캡쳐 기능

오류 발생시 스크린 캡쳐 기능을 켜고 끌 수 있다. (체크 시: 스크린 캡쳐 기능 끄기)

6) 멀티 태스크 설정 기능

멀티 태스크 설정을 통해 동시에 여러 개의 태스크를 실행할 수 있다. (멀티 태스크를 사용하는 경우 마우스, 키보드 또는 이미지 등 같은 객체를 사용할 경우 주의가 필요하다.)

7) 로그 레벨 설정 기능

Full, Normal, Simple, OneLine, None의 로그 레벨 설정을 설정하는 기능이다.

8) 저장 폴더 변경 기능

태스크 저장 폴더를 설정하는 기능이다.(현재 비활성화)

9) 세팅 백업 기능

현재 설정을 파일로 백업하는 기능이다.

10) 세팅 롤백 기능

백업된 설정 파일을 불러오는 기능이다.

##### 4-4-2. Assist 큐 설정 기능

![250_assist_img21](../assets/ba-assist/2.5.0/250_assist_img21-0a40a1b2d7fdc850.png)

☞ Queue 기능은 Sequential 태스크와 함께 사용할 수 없습니다. Sequential 태스크는 Queue에 쌓이지 않습니다.

[Setting Queue Configuration 탭 참조]

1) Use Queue (Automatic)

Task Watcher라는 툴을 이용하여 자동으로 Queue에 쌓인 Task를 실행하는 방식

- 1) Watch Interval : 이미 동작중인 Task가 종료되는 것을 체크하는 기간 설정
- 2) Watcher run time : Task Watcher가 동작하는 시간을 설정 ▶ Always : 항상 Task Watcher가 동작한다. - 이전 Task가 끝나면 다음 큐 Task를 시작할 시간을 설정해야한다.
- - Set start program : 프로그램을 시작하면 자동으로 Task Watcher가 동작한다.
- ▶ Set time: Task Watcher가 설정한 시작 시간 ~ 끝 시간만큼 동작한다.
2) Use Queue (Manual)

Queue Task List의 있는 Task를 수동으로 실행하는 방식

![assist_img35](../assets/ba-assist/2.5.0/assist_img35-a1522e602a8a98e9.png)

- Queue Task List : 큐에 쌓여있는 Task의 목록을 확인할 수 있는 창.

1)Run Task: 설정한 시간 뒤에 선택한 Task를 실행한다.

2)Search : 특정 Task를 검색한다.

3)Delete executed tasks: 실행된 Task를 리스트에서 제거한다.

4)Delete : 선택된 Task를 Queue에서 제거한다.

#### 4-5. Packing

Main 탭의 기능은 서버에 등록된 패키지 리스트와 Local Repository의 폴더/파일 리스트를 갱신한다. 상세한 설명은 3-1-2. fpk 파일 변환 후, 로딩하기를 참조한다.

![assist_img46_6](../assets/ba-assist/2.5.0/assist_img46_6-9ab3ce37331633bf.png)

#### 4-6. Sequential

Main 탭의 기능은 연속 태스크 실행을 설정하는 창을 오픈한다.

![250_assist_img22](../assets/ba-assist/2.5.0/250_assist_img22-bb693854328f0be3.png)

![250_assist_img24](../assets/ba-assist/2.5.0/250_assist_img24-76110d2bc074fe95.png)

Sequential 태스크는 여러 개의 태스크를 순차적으로 실행하는 기능이다. 태스크를 추가하고 삭제할 수 있으며, 순서를 변경할 수 있다. 태스크를 추가하려면 Add Sequential 버튼을 클릭하고, 삭제하려면 X 버튼을 클릭한다.

Sequential Task 설정 방법

| Add Sequential 버튼을 클릭한다. | Sequential Task의 이름과 설명을 작성한다. | 순차적 실행을 원하는 태스크를 순서대로 Drag & Drop 한다. |
| --- | --- | --- |

#### 4-7. Refresh

Main 탭의 기능은 서버에 등록된 패키지 리스트와 Local Repository의 폴더/파일 리스트를 갱신한다.

![assist_img46_7](../assets/ba-assist/2.5.0/assist_img46_7-d080abc098d64dd7.png)

#### 4-8. Close

Main 탭의 기능은 BA Assist 창을 최소화한다.(BA Assist는 백그라운드 계속한다.)

![assist_img46_8](../assets/ba-assist/2.5.0/assist_img46_8-27c05a1b08234c94.png)

#### 4-9. Exit

Main 탭의 기능은 BA Assist를 완전히 종료한다.

![assist_img46_9](../assets/ba-assist/2.5.0/assist_img46_9-2fc713de1e88686d.png)

#### 4-10. Help

Main 탭의 기능은 6-4. Help 메뉴를 참조한다.

![assist_img46_10](../assets/ba-assist/2.5.0/assist_img46_10-8849b9b250058df9.png)

05. 시스템 트레이 메뉴

윈도우 화면 우측하단의 시스템 트레이에서 BA Assist가 구동되고 있는 것을 확인할 수 있다. 메뉴 아이콘에 커서를 대고 마우스 오른쪽을 누르면 BA Assist의 메인 화면으로 이동하거나 특정한 부가기능으로 이동하는 기능, 백그라운드 모드로 동작하던 BA Assist를 종료하는 기능 등을 이용할 수 있다. BA Assist에서 제공하는 트레이 메뉴 리스트는 아래와 같다.

| ▶▶ |
| --- |

각 메뉴 항목 선택 시 수행되는 기능을 정리하면 아래와 같다.

#### 5-1. Task List

태스크의 리스트가 나타나는 BA Assist의 메인 화면으로 이동한다.

#### 5-2. Logs

태스크의 수행시간, 성공여부, 로그 메시지 등의 수행이력을 보여주는 로그 화면창이 나타난다.

#### 5-3. Task Schedule

상태 보기 메뉴에서 Schedule아이콘을 누르면 나타나는 기능으로, 태스크 리스트에서 아이콘으로 표시되어 있는 스케줄이 설정된 태스크들을 추출하여 보여주는 화면이 나타난다.

![b_img08](../assets/ba-assist/2.5.0/b_img08-ce8a8358d85d0c36.png)

#### 5-4. Setting

서버 접속정보를 설정하고 접속 시험을 수행하는 기능, 태스크의 저장 폴더를 변경하는 기능, 키보드와 마우스를 제어하는 기능, 대시보드 사용 기능 등을 제공하는 BA Assist Setting 창이 나타난다. 상세 설명은 4-4. Setting 을 참조한다.

#### 5-5. Exit

BA Assist를 종료하는 기능을 수행한다. BA Assist 우측 상단의(Close) 버튼을 누르면 Foreground의 BA Assist는 닫히지만 BA Assist는 여전히 Background모드로 동작하고 있다. 이는 스케줄 이벤트가 발생할 경우 처리해야 하기 때문이다. 따라서 BA Assist를 완전히 종료하고자 할 때는 바로 이 시스템 트레이 메뉴에 있는 BA Assist의 Exit를 클릭하여 종료한다.

![b_img06](../assets/ba-assist/2.5.0/b_img06-2bc337ed709d31d0.png)

06. 기타 편의기능

#### 6-1. 스킨 설정 기능

아래의 화면처럼 View 메뉴에서 아래 방향키를 누르면 다양한 스킨 선택 메뉴 리스트가 나타나고 그 중 하나를 선택하여 GUI 화면의 스킨을 변경할 수 있다.

![assist_img37](../assets/ba-assist/2.5.0/assist_img37-8c67603354dfd842.png)

#### 6-2. View Panel 기능

Tasks, Logs, Dashboard, Schedule 패널은 사용자의 의도에 의해서 화면 보기 구성에서 사라지고 나타나게 할 수 있다. 아래의 그림은 메인 메뉴에서 View를 클릭하면 나타나는 화면이다. 여기서 우측 Panel 탭에 보이는 Tasks, Logs, Dashboard, Schedule 패널은 마우스 클릭 시 활성화 할 수 있다.

![assist_img38](../assets/ba-assist/2.5.0/assist_img38-4b052d45f69312da.png)

화면 왼쪽에 위치한 Task, Logs 패널, 우측에 위치한 Dashboard, Schedule 패널은 각각 을 눌러 닫게 할 수 있고 닫힌 패널 항목은 View Panel 메뉴의 항목을 클릭하여 다시 활성화하여 보이게 할 수 있다.

![b_img06](../assets/ba-assist/2.5.0/b_img06-2bc337ed709d31d0.png)

#### 6-3. 리본메뉴 최소화(Ctrl+F1)

BA Assist화면에서 하단의 컨텐츠 영역 확대를 위하여 상단의 메인 메뉴에 해당하는 리본 메뉴의 상하 크기를 줄일 수 있는 (리본메뉴 최소화) 수단을 제공하고 있다. 리본메뉴의 최소화 설정은 컨텐츠 영역 우측 상단의 아이콘을 클릭하여 수행할 수 있으며,

![b_img07](../assets/ba-assist/2.5.0/b_img07-35187a3d5c00c3d7.png)

![assist_img39](../assets/ba-assist/2.5.0/assist_img39-5ebca9e7beb9886c.png)

리본메뉴 최소화는 Ctrl+F1을 통해 최소화된 리본메뉴를 해제하여 리본메뉴를 최대화 할 수 있다. ☞ 아이콘 클릭 없이 단축키를 통하여 Ctrl+F1을 누름으로 리본메뉴 최소화 설정과 해제를 번갈아 가며 수행할 수 있다.

#### 6-4. Help 메뉴

BA Assist화면 상단 메인 메뉴에서 를 클릭하면 아래처럼 5가지의 Help메뉴가 표시된다.

![assist_img46_10](../assets/ba-assist/2.5.0/assist_img46_10-8849b9b250058df9.png)

![250_assist_img27](../assets/ba-assist/2.5.0/250_assist_img27-2ef7d775f3af0107.png)

각 메뉴에서 제공되는 기능은 다음과 같다.

| Help Menu | 설명 |
| --- | --- |
| About | BA Assist의 Release Version을 표시한다. |
| License | License파일의 등록과 관리 기능을 제공한다. |
| Manual | BA Assist 사용자 매뉴얼을 열람할 수 있다. |
| Request License | BA Assist 라이선스를 요청하는 페이지를 연다. |
| DB Schema update | BA Assist 데이터 베이스 스키마를 최신화한다. |

## Source Code Supplement

이 섹션은 BA-Assist 실제 소스 `E:\BATEM_RPA\BA-Assist\FreeperAssist`를 확인하여 온라인 매뉴얼의 누락되기 쉬운 운영 정보를 보강한 내용이다.

### Source-Confirmed Package Loading

- 패키지 정보 로딩은 `Modules/BizData.cs`에서 확인된다.
- Repository Path에서 패키지 파일 목록을 읽고 각 파일을 `PackkageInfo`로 변환한다.
- BA-Assist는 `*.fpk`와 `*.fpx` 패키지를 모두 읽는다.
- `*.fpx` 파일이면 프로젝트 패키지로 처리하며, 압축을 임시 폴더에 풀고 내부 `fpp` 파일을 찾아 시작 태스크와 패키지 이름/설명을 읽는다.
- `*.fpk` 파일이면 태스크 패키지로 처리하며, 압축을 임시 폴더에 풀고 `config.json`에서 `main`, `packageName`, `packageDesc`를 읽는다.
- 패키지 분석 후 임시 폴더는 삭제된다.

### Source-Confirmed Task Update

- 태스크 업데이트 화면은 `UI/TaskUpdate.cs`에서 확인된다.
- 업데이트 파일 선택 필터는 `*.fpx`와 `*.fpk`를 모두 허용한다.
- 기존 패키지 파일명과 새로 선택한 파일명이 다르면 업데이트를 막고 파일명을 확인하라고 안내한다.
- 업데이트 시 패키지 설명과 갱신 날짜 정보가 `PackkageInfo`에 반영되고 패키지 정보 저장 로직이 호출된다.

### Source-Confirmed Scheduler Behavior

- Windows Task Scheduler 연동은 `Modules/TaskScheduler_config.cs`에서 확인된다.
- BA-Assist 스케줄은 Windows 작업 스케줄러의 `BA-Assist` 폴더 아래에 등록된다.
- 스케줄 실행 Action은 BA-Assist 플레이어 실행 파일과 패키지 파일 경로를 인자로 사용한다.
- 관리자 권한으로 실행 중이면 `BUILTIN\Users` 그룹과 Highest run level을 사용하고, 일반 사용자 환경에서는 현재 사용자 InteractiveToken을 사용한다.
- 스케줄 설명, 활성 상태, 트리거, 다중 인스턴스 정책이 작업 정의에 반영된다.
- 실행 후 PC 종료 옵션이 켜져 있으면 shutdown action이 추가될 수 있다.
- 기존 스케줄 수정 시 트리거 개수와 내용, shutdown action, 다중 인스턴스 정책을 갱신한다.

### Source-Confirmed Trigger and Multi-Instance Options

- 스케줄 모델은 `Modules/TaskScheduleler_Info.cs`에서 확인된다.
- Trigger 유형은 `OneTime`, `Daily`, `Weekly`, `Monthly`를 지원한다.
- Weekly는 요일 값을 사용하고, Monthly는 특정 날짜 또는 특정 주차/요일 방식을 지원한다.
- 반복 실행 옵션은 반복 간격과 반복 유지 시간을 가진다.
- 반복 간격 단위는 `minute`, `hour`, `day`를 지원한다.
- 다중 인스턴스 정책은 `IgnoreNew`, `Parallel`, `Queue`, `StopExisting`를 지원한다.
- 화면/답변에서 "새 인스턴스를 실행 안함", "인스턴스를 병렬로 실행", "새 인스턴스 대기", "기존 인스턴스 중지"로 설명할 수 있다.

### Source-Confirmed Settings and Repository Behavior

- 설정 로딩/저장은 `Modules/Settings.cs`에서 확인된다.
- Local Repository Root와 RootName, BA-Server 접속 정보, Queue 사용 여부, Queue 옵션, 로그 파일 보관 기간을 config 테이블에서 읽고 쓴다.
- 서버 프로토콜 기본값은 `http`, 서버 포트 기본값은 `8080`이다.
- Queue 옵션이 비어 있으면 기본값으로 `Both`를 사용한다.
- 화면 설정 일부는 WinForms `Properties.Settings`에도 저장된다.
- 스케줄 실행 시 필요한 키보드 입력 차단, 오류 화면 캡처 설정도 config에 저장된다.

### Source-Confirmed Package Status and Schedule Display

- 패키지 목록 모델은 `Modules/PackkageInfo.cs`에서 확인된다.
- 패키지 항목은 파일명, 태스크 이름, 갱신일, 갱신자, 다운로드일, 단축키, 전체 경로, 설명, 스케줄 정보를 가진다.
- 스케줄이 있고 활성 Trigger가 있으면 다음 실행 시간이 표시된다.
- 스케줄 종류는 활성 Trigger의 `ScType_Text` 또는 `ScType` 값으로 표시된다.
- Tooltip에는 Schedule Trigger, Next run time, Last run time, Last task result가 표시될 수 있다.

### Source-Confirmed Task Variables

- 패키지 변수 편집은 `UI/TaskVariables.cs`에서 확인된다.
- 패키지를 임시 폴더에 압축 해제하고 `Variables.json`을 읽어 글로벌 변수 목록을 만든다.
- 저장 시 변경된 변수는 다시 `Variables.json`의 `globals` 배열로 기록된다.
- 변수 항목은 `type`, `key`, `value`를 가진다.
- Password 타입 값은 화면 표시 시 마스킹된다.
- 패키지 편집 완료 후 임시 폴더는 삭제된다.

### Source References

- `E:\BATEM_RPA\BA-Assist\FreeperAssist\Modules\BizData.cs`
- `E:\BATEM_RPA\BA-Assist\FreeperAssist\Modules\TaskScheduler_config.cs`
- `E:\BATEM_RPA\BA-Assist\FreeperAssist\Modules\TaskScheduleler_Info.cs`
- `E:\BATEM_RPA\BA-Assist\FreeperAssist\Modules\Settings.cs`
- `E:\BATEM_RPA\BA-Assist\FreeperAssist\Modules\PackkageInfo.cs`
- `E:\BATEM_RPA\BA-Assist\FreeperAssist\UI\TaskUpdate.cs`
- `E:\BATEM_RPA\BA-Assist\FreeperAssist\UI\TaskVariables.cs`

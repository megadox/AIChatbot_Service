## Source Code Supplement

이 섹션은 BA-Assist 실제 소스 `E:\BATEM_RPA\BA-Assist\FreeperAssist`를 확인하여 온라인 매뉴얼의 누락되기 쉬운 운영 정보를 보강한 내용이다.

### Source-Confirmed Package Loading

- 패키지 정보 로딩은 `Modules/BizData.cs`에서 확인된다.
- Repository Path에서 패키지 파일 목록을 읽고 각 파일을 `PackkageInfo`로 변환한다.
- BA-Assist는 `*.fpk`와 `*.fpx` 패키지를 모두 읽는다.
- `*.fpx` 파일이면 프로젝트 패키지로 처리하며, 압축을 임시 폴더에 풀고 내부 `fpp` 파일을 찾아 시작 태스크와 패키지 이름/설명을 읽는다.
- `*.fpk` 파일이면 태스크 패키지로 처리하며, 압축을 임시 폴더에 풀고 `config.json`에서 `main`, `packageName`, `packageDesc`를 읽는다.
- 패키지 분석 후 임시 폴더는 삭제된다.

### Source-Confirmed Task Update

- 태스크 업데이트 화면은 `UI/TaskUpdate.cs`에서 확인된다.
- 업데이트 파일 선택 필터는 `*.fpx`와 `*.fpk`를 모두 허용한다.
- 기존 패키지 파일명과 새로 선택한 파일명이 다르면 업데이트를 막고 파일명을 확인하라고 안내한다.
- 업데이트 시 패키지 설명과 갱신 날짜 정보가 `PackkageInfo`에 반영되고 패키지 정보 저장 로직이 호출된다.

### Source-Confirmed Scheduler Behavior

- Windows Task Scheduler 연동은 `Modules/TaskScheduler_config.cs`에서 확인된다.
- BA-Assist 스케줄은 Windows 작업 스케줄러의 `BA-Assist` 폴더 아래에 등록된다.
- 스케줄 실행 Action은 BA-Assist 플레이어 실행 파일과 패키지 파일 경로를 인자로 사용한다.
- 관리자 권한으로 실행 중이면 `BUILTIN\Users` 그룹과 Highest run level을 사용하고, 일반 사용자 환경에서는 현재 사용자 InteractiveToken을 사용한다.
- 스케줄 설명, 활성 상태, 트리거, 다중 인스턴스 정책이 작업 정의에 반영된다.
- 실행 후 PC 종료 옵션이 켜져 있으면 shutdown action이 추가될 수 있다.
- 기존 스케줄 수정 시 트리거 개수와 내용, shutdown action, 다중 인스턴스 정책을 갱신한다.

### Source-Confirmed Trigger and Multi-Instance Options

- 스케줄 모델은 `Modules/TaskScheduleler_Info.cs`에서 확인된다.
- Trigger 유형은 `OneTime`, `Daily`, `Weekly`, `Monthly`를 지원한다.
- Weekly는 요일 값을 사용하고, Monthly는 특정 날짜 또는 특정 주차/요일 방식을 지원한다.
- 반복 실행 옵션은 반복 간격과 반복 유지 시간을 가진다.
- 반복 간격 단위는 `minute`, `hour`, `day`를 지원한다.
- 다중 인스턴스 정책은 `IgnoreNew`, `Parallel`, `Queue`, `StopExisting`를 지원한다.
- 화면/답변에서 "새 인스턴스를 실행 안함", "인스턴스를 병렬로 실행", "새 인스턴스 대기", "기존 인스턴스 중지"로 설명할 수 있다.

### Source-Confirmed Settings and Repository Behavior

- 설정 로딩/저장은 `Modules/Settings.cs`에서 확인된다.
- Local Repository Root와 RootName, BA-Server 접속 정보, Queue 사용 여부, Queue 옵션, 로그 파일 보관 기간을 config 테이블에서 읽고 쓴다.
- 서버 프로토콜 기본값은 `http`, 서버 포트 기본값은 `8080`이다.
- Queue 옵션이 비어 있으면 기본값으로 `Both`를 사용한다.
- 화면 설정 일부는 WinForms `Properties.Settings`에도 저장된다.
- 스케줄 실행 시 필요한 키보드 입력 차단, 오류 화면 캡처 설정도 config에 저장된다.

### Source-Confirmed Package Status and Schedule Display

- 패키지 목록 모델은 `Modules/PackkageInfo.cs`에서 확인된다.
- 패키지 항목은 파일명, 태스크 이름, 갱신일, 갱신자, 다운로드일, 단축키, 전체 경로, 설명, 스케줄 정보를 가진다.
- 스케줄이 있고 활성 Trigger가 있으면 다음 실행 시간이 표시된다.
- 스케줄 종류는 활성 Trigger의 `ScType_Text` 또는 `ScType` 값으로 표시된다.
- Tooltip에는 Schedule Trigger, Next run time, Last run time, Last task result가 표시될 수 있다.

### Source-Confirmed Task Variables

- 패키지 변수 편집은 `UI/TaskVariables.cs`에서 확인된다.
- 패키지를 임시 폴더에 압축 해제하고 `Variables.json`을 읽어 글로벌 변수 목록을 만든다.
- 저장 시 변경된 변수는 다시 `Variables.json`의 `globals` 배열로 기록된다.
- 변수 항목은 `type`, `key`, `value`를 가진다.
- Password 타입 값은 화면 표시 시 마스킹된다.
- 패키지 편집 완료 후 임시 폴더는 삭제된다.

### Source References

- `E:\BATEM_RPA\BA-Assist\FreeperAssist\Modules\BizData.cs`
- `E:\BATEM_RPA\BA-Assist\FreeperAssist\Modules\TaskScheduler_config.cs`
- `E:\BATEM_RPA\BA-Assist\FreeperAssist\Modules\TaskScheduleler_Info.cs`
- `E:\BATEM_RPA\BA-Assist\FreeperAssist\Modules\Settings.cs`
- `E:\BATEM_RPA\BA-Assist\FreeperAssist\Modules\PackkageInfo.cs`
- `E:\BATEM_RPA\BA-Assist\FreeperAssist\UI\TaskUpdate.cs`
- `E:\BATEM_RPA\BA-Assist\FreeperAssist\UI\TaskVariables.cs`
